export interface TableRecordProps {
    id: number;
    date: string;
    capacity: string;
    code: string;
  }

export  interface FlightFormInputs {
    code: string;
    date: Date;
    capacity: string;
  }
  